// examples/demo.ltl — runnable demo for ltl_semver

import std::io
use ltl_semver as lib

fn main() {
    io::println("ltl_semver demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}